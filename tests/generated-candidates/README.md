# Generated test candidates

YAML/JSON here is **speculative**. CI must not collect this directory.

Promotion path: candidate → strength gate (baseline pass + targeted fault
fail) → stability → traceability link → `tests/` (verified regression) or
`tests/adversarial/` (extended resilience).

Schema: `tests/contracts/test-candidate.schema.json`  
Example: `docs/testing/test-candidate.example.yaml`
